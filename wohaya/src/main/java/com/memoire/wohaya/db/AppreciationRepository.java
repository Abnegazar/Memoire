/*
package com.memoire.wohaya.db;

import com.memoire.wohaya.model.Appreciation;
import com.memoire.wohaya.model.Publication;
import com.memoire.wohaya.model.Utilisateur;
import org.springframework.data.jpa.db.JpaRepository;
import org.springframework.lang.Nullable;

public interface AppreciationRepository extends JpaRepository<Appreciation, Long> {

    @Nullable
    Appreciation findByAuteurAndPublication(Utilisateur auteur, Publication publication);

    @Nullable
    boolean existByAuteurAndPublication(Utilisateur auteur, Publication publication);

}
*/
